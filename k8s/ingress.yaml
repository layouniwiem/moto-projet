apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: moto-app-ingress
  namespace: moto-app
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/use-regex: "true"
    # Configuration spécifique pour /metrics
    nginx.ingress.kubernetes.io/configuration-snippet: |
      location = /metrics {
        proxy_pass http://flask-app.moto-app.svc.cluster.local:5000;
        auth_basic off;
        proxy_ssl_verify off;
      }
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - moto-app.example.com  # Uniquement le domaine principal
    secretName: moto-app-tls
  rules:
  - host: moto-app.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: flask-app
            port:
              number: 80
      - path: /metrics
        pathType: Exact  # Correspondance exacte
        backend:
          service:
            name: flask-app
            port:
              number: 5000